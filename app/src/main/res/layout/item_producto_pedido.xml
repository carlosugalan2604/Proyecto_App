<?xml version="1.0" encoding="utf-8"?>
<!-- Fila de producto para la pantalla "Nuevo Pedido".
     Muestra el nombre, precio y un control numérico (- / número / +) para la cantidad. -->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:padding="16dp"
    android:gravity="center_vertical"
    android:background="#FFFFFF">

    <!-- Información del producto (nombre + precio) -->
    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical">

        <TextView
            android:id="@+id/tvNombreProducto"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Nombre del producto"
            android:textSize="14sp"
            android:textStyle="bold"
            android:textColor="#212121" />

        <TextView
            android:id="@+id/tvPrecioProducto"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="€ 0.00"
            android:textSize="12sp"
            android:textColor="#757575"
            android:layout_marginTop="2dp" />
    </LinearLayout>

    <!-- Control de cantidad: botón menos, número, botón más -->
    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:background="@drawable/bg_qty_control"
        android:paddingStart="4dp"
        android:paddingEnd="4dp">

        <Button
            android:id="@+id/btnMenos"
            android:layout_width="36dp"
            android:layout_height="36dp"
            android:text="−"
            android:textSize="18sp"
            android:textColor="#00CC99"
            android:backgroundTint="@android:color/transparent"
            android:padding="0dp" />

        <TextView
            android:id="@+id/tvCantidad"
            android:layout_width="36dp"
            android:layout_height="wrap_content"
            android:text="0"
            android:textSize="15sp"
            android:textStyle="bold"
            android:textColor="#212121"
            android:gravity="center"
            android:textAlignment="center" />

        <Button
            android:id="@+id/btnMas"
            android:layout_width="36dp"
            android:layout_height="36dp"
            android:text="+"
            android:textSize="18sp"
            android:textColor="#00CC99"
            android:backgroundTint="@android:color/transparent"
            android:padding="0dp" />
    </LinearLayout>

</LinearLayout>
